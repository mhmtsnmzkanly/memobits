// Struct ve template interpolasyon ({id} desteklenir)
struct Player {
    name: String,
    score: Int
}

let p = Player { name: "Alice", score: 100 };
let n = p.name;
let s = p.score;
native::print(`Player {n} has score {s}`);
